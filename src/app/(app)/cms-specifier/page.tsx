import { CMSSpecifier } from "@/components/cms-specifier/CMSSpecifier";

export const metadata = { title: "CMS Specifier — MoonTasks" };

export default function CMSSpecifierPage() {
  return <CMSSpecifier />;
}
