import { Annotator } from "@/components/annotator/Annotator";

export const metadata = { title: "New Feature — Spec Annotator" };

export default function NewAnnotatorPage() {
  return <Annotator />;
}
